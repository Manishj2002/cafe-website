# .env          (or .env.local)  → used in development (npm run dev)
VITE_API_BASE_URL=http://localhost:5000
# .env.production  → optional, but Vercel/Netlify/Render will override it anyway
VITE_API_BASE_URL=https://cafe-website-y86g.onrender.com